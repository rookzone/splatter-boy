#ifndef SCENE_SCORE_SCREEN_H
#define SCENE_SCORE_SCREEN_H

// Initializes the score scene
void init_score_scene(void);

// Updates the score scene
void update_score_scene(void);

// Cleans up the score scene
void cleanup_score_scene(void);

#endif // SCENE_SCORE_SCREEN_H
